name: seedhabazaar
description: "A new Flutter project."
publish_to: 'none' # Change to 'https://pub.dev' if you want to publish the package

version: 1.0.0+1

environment:
  sdk: ">=3.7.0-209.1.beta <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  file_picker: ^6.1.1
  fl_chart: ^0.45.1
  razorpay_flutter: ^1.3.5
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  

flutter:
  uses-material-design: true

  assets:
    - assets/products.json
    # - assets/images/ # Uncomment if using image assets

  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
